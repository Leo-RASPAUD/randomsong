type Song @model @auth(rules: [{ allow: private, provider: userPools }, { allow: public, provider: iam }]) {
  id: ID!
  name: String!
  description: String
  difficulty: String!
  band: String!
  style: String
}
